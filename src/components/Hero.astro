---
// src/components/Hero.astro
// ── EDIT HERE ──
// Replace [Your Name] with your real name
// Replace GitHub/LinkedIn/email href values with your actual links
---

<section id="hero" class="hero">
  <div class="container hero-inner">

    <!-- Eyebrow label -->
    <p class="hero-eyebrow">
      <span class="dot"></span> Available for internships
    </p>

    <!-- Name — REPLACE [Your Name] -->
    <h1 class="hero-name">
      <span class="name-line">Hi, I'm</span>
      <span class="name-highlight">Gustavo Toste</span>
    </h1>

    <!-- Title -->
    <p class="hero-title">
      Computer Engineering Student <span class="at">@</span> UCF
    </p>

    <!-- Short bio -->
    <p class="hero-bio">
      I build things for the web and beyond — from responsive interfaces to
      C programs and Python tools. Currently a freshman at UCF, learning fast,
      and looking for opportunities to contribute to real-world software projects.
    </p>

    <!-- CTA buttons -->
    <!-- Replace href values with your actual links -->
    <div class="hero-ctas">
      <a href="/resume.pdf" target="_blank" rel="noopener" class="btn btn-primary">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
          <polyline points="14,2 14,8 20,8"/><line x1="16" y1="13" x2="8" y2="13"/>
          <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10,9 9,9 8,9"/>
        </svg>
        Resume
      </a>
      <a href="https://github.com/yourusername" target="_blank" rel="noopener" class="btn btn-outline">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 0C5.37 0 0 5.373 0 12c0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61-.546-1.385-1.335-1.755-1.335-1.755-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23A11.52 11.52 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.29-1.552 3.297-1.23 3.297-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 21.795 24 17.298 24 12c0-6.627-5.373-12-12-12z"/>
        </svg>
        GitHub
      </a>
      <a href="https://linkedin.com/in/yourprofile" target="_blank" rel="noopener" class="btn btn-outline">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor">
          <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
        </svg>
        LinkedIn
      </a>
      <a href="#contact" class="btn btn-outline">
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
          <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
          <polyline points="22,6 12,13 2,6"/>
        </svg>
        Contact
      </a>
    </div>

    <!-- Scroll hint -->
    <div class="hero-scroll-hint">
      <span>scroll</span>
      <div class="scroll-line"></div>
    </div>

  </div>

  <!-- Background decoration -->
  <div class="hero-glow" aria-hidden="true"></div>
</section>

<style>
  .hero {
    min-height: 100svh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding-top: 5rem;
  }

  .hero-glow {
    position: absolute;
    top: -10%;
    right: -15%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(91,143,255,0.08) 0%, transparent 65%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    max-width: 720px;
  }

  /* Eyebrow */
  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--text-muted);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 1.4rem;
    animation: fadeUp 0.5s ease both;
  }
  .dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: #4ade80;
    box-shadow: 0 0 8px #4ade80;
    animation: pulse 2.5s ease infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%       { opacity: 0.5; transform: scale(0.85); }
  }

  /* Name */
  .hero-name {
    display: flex;
    flex-direction: column;
    font-size: clamp(2.8rem, 7vw, 5.5rem);
    font-weight: 800;
    line-height: 1.05;
    margin-bottom: 1rem;
    animation: fadeUp 0.55s 0.1s ease both;
  }
  .name-line {
    color: var(--text-secondary);
    font-size: 0.42em;
    font-weight: 500;
    letter-spacing: 0.04em;
    font-family: var(--font-mono);
    margin-bottom: 0.15em;
  }
  .name-highlight {
    background: linear-gradient(135deg, var(--text-primary) 40%, var(--accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Title */
  .hero-title {
    font-size: clamp(1rem, 2.5vw, 1.35rem);
    font-weight: 500;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    animation: fadeUp 0.6s 0.2s ease both;
    font-family: var(--font-mono);
  }
  .at { color: var(--accent); font-style: normal; }

  /* Bio */
  .hero-bio {
    font-size: clamp(0.95rem, 1.5vw, 1.1rem);
    color: var(--text-secondary);
    max-width: 560px;
    line-height: 1.75;
    margin-bottom: 2.5rem;
    animation: fadeUp 0.6s 0.3s ease both;
  }

  /* CTAs */
  .hero-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    animation: fadeUp 0.6s 0.4s ease both;
  }

  /* Scroll hint */
  .hero-scroll-hint {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 4rem;
    animation: fadeUp 0.6s 0.55s ease both;
  }
  .hero-scroll-hint span {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-muted);
  }
  .scroll-line {
    width: 40px; height: 1px;
    background: var(--border-light);
    position: relative;
    overflow: hidden;
  }
  .scroll-line::after {
    content: '';
    position: absolute;
    left: -100%;
    width: 100%; height: 100%;
    background: var(--accent);
    animation: slideLine 2s ease infinite;
  }
  @keyframes slideLine {
    0%   { left: -100%; }
    60%  { left: 100%; }
    100% { left: 100%; }
  }

  /* Responsive */
  @media (max-width: 600px) {
    .hero-ctas { gap: 0.6rem; }
    .btn { font-size: 0.82rem; padding: 0.55rem 1rem; }
  }
</style>
